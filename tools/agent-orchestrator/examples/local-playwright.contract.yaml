schema_version: 1

goal:
  id: local-browser-goal
  title: Verify the local browser flow
  requirement: The approved user journey works against a disposable local service.
  acceptance:
    - id: AC-1
      statement: The browser completes the approved user journey.

approval:
  status: approved
  approved_by: owner
  approved_at: 2026-07-15T00:00:00Z

agent:
  provider: codex

project:
  repo: /absolute/path/to/trusted/project
  base_ref: main

todo:
  id: T-1
  title: Implement and verify the browser journey

test:
  command: [npx, playwright, test]
  allowed_paths: [e2e/**]
  timeout_seconds: 900
  harness: local-browser
