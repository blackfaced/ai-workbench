interface:
  display_name: "Handoff to Dev"
  short_description: "核对工作快照与目标环境，把当前任务安全交接到指定开发机"
  default_prompt: "使用 $handoff-to-dev 处理当前项目的相关任务，先核对事实、范围与已有授权。"
policy:
  allow_implicit_invocation: false
