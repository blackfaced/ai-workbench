interface:
  display_name: "Implement Batch"
  short_description: "确认范围与模型，以 master-worker-QA 小切片闭环实施和验收"
  default_prompt: "使用 $implement-batch 实施这个 spec，先核对已有实现，再展示首个可验证切片、QA 分工、模型、返修预算和权限供我确认。"
policy:
  allow_implicit_invocation: false
